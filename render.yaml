services:
  - type: web
    name: relay-med-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: GEMINI_API_KEY
        sync: false
      - key: LLM_PROVIDER
        value: "gemini"
      - key: CORS_ORIGINS
        value: "*"
      # Auth: set a strong random JWT secret and (optionally) a Google client ID.
      - key: SECUREMED_JWT_SECRET
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        sync: false
